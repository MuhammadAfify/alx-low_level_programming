#include <stdio.h>
/**
 * main - prints all possible combinations of single-digit numbers
 *
 * Return: Always 0.
 */
#include <stdio.h>
int main(void)
{
	int i;
	char letter;

	for (i = 0 ; i<= 0 ; i++)
	{
		putchar((i % 10) + '0');
		putchar(', ');
	}
	putchar('\n');
	return (0);
}
