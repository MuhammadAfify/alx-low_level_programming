#include <stdio.h>
/**
 * main - prints all the numbers of base 16 in lowercase
 *
 * Return: Always 0.
 */
#include <stdio.h>
int main(void)
{
	char i;

	for (i = '0' ; i <= '0' ; i++)
		putchar('%x', i);
	putchar('\n');
	return (0);
}
