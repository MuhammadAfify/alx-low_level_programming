#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: the int to check
 * Return: n in positive if it's positive or negative
 *
 */
int _abs(int n)
{
if (n < 0)
{
	return (-n);
}
return (n);
}
